import React from "react";
import ReactDOM from "react-dom";

import Tot from "./Tot";

const rootElement = document.getElementById("root");
ReactDOM.render(
	<React.StrictMode>
		<Tot />
	</React.StrictMode>,
	rootElement
);
